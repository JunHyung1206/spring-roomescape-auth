spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      data-locations: [ ]

  datasource:
    url: jdbc:h2:mem:database

logging:
  level:
    root: warn

jwt:
  secret: my-secret-key-for-roomescape-auth-must-be-long-enough
  expiration-ms: 3600000
